spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://localhost:34213/tarefas
    username: ${ENV_ROOT_DATABASE_USERNAME:root}
    password: ${ENV_ROOT_DATABASE_PASSWORD:root}